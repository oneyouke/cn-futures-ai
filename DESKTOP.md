# Windows 桌面版

桌面版提供中文窗口、合成行情演示、CSV 行情导入、JSON 合约配置选择、权益曲线、成交记录及完整结果导出。当前是离线研究工具，不连接恒力期货账户，不发送订单，尚无 AI 模型。

## 下载免 Python 版本

打开仓库 Actions，选择最新成功的 Windows desktop 运行。在 Artifacts 下载 FuturesDesk-Windows-x64，解压后双击 FuturesDesk.exe。下载构建产物需要登录 GitHub。构建产物保留 30 天。此版本未做代码签名。

## 使用

1. 点击「运行演示」，查看合成行情结果。
2. 在「权益曲线」「成交记录」页签切换。
3. 导入真实数据时，选择行情 CSV 和合约 JSON。可通过「保存配置示例」生成配置，再按真实合约修改。
4. 点击「回测所选行情」。格式和模型边界详见 README.md 及软件「使用说明」。
5. 点击「导出结果」并选择保存目录。每次生成独立文件夹，包含 summary.json、trades.csv、equity.csv。

演示按钮固定使用演示参数，不使用所选真实配置。图表含初始资金点；表格最多显示 5,000 笔成交，导出保存全部。后台计算期间运行按钮禁用；关闭窗口将结束当前计算。

## 源码运行

安装含 Tkinter 的 Python 3.10 或更新版本，在项目目录运行：

```bat
python desktop.py
```

## 开发验证

Windows 构建流程先测试回测引擎、窗口创建、曲线/成交显示、CSV 导入和结果导出，再生成 exe 并对 exe 重复桌面冒烟测试。

这不代表真实账户接入或盈利能力已经验证。实际客户端系统兼容性以用户电脑验证为准。
